<div {{attributes}}>
	<div {{ swipper_attributes.addClass(['swiper','carousel-hero']) }}>
		<div class="swiper-wrapper">
			{% for row in rows %}
			<div {{row.attributes}}>
				{{ row|without('attributes') }}
			</div>
			{% endfor %}
		</div>
		<!-- If we need pagination -->
		<div class="swiper-pagination carousel-nav"></div>
	</div>
</div>